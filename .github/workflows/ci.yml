name: CI

# Lint + tests on every push and PR. Without this, lint errors only surfaced
# when somebody happened to run `./make.sh format` locally — which is how 144 of
# them accumulated unnoticed.
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      # One entrypoint, so CI and a local `./make.sh lint` can never drift
      # apart. What it runs (ruff + ty here) is the service's business — the
      # pipeline only knows the verb, which is what keeps it language-agnostic.
      # ruff and ty are both pinned in pyproject.toml, so the rule set is fixed.
      - name: Lint
        run: ./make.sh lint
        env:
          # Renders ruff findings as inline PR annotations without make.sh
          # needing to know it is running inside GitHub Actions.
          RUFF_OUTPUT_FORMAT: github

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - name: Run tests
        run: ./make.sh test
